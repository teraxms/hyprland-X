# Hyprland by Terax 👾

Добро пожаловать в мою конфигурацию **Hyprland**! Эта настройка создавалась для того, чтобы обеспечить красивый, плавный и современный рабочий опыт на **Wayland**, сохраняя при этом производительность и гибкость.

## ✅ Зависимости

* **Менеджер окон:** Hyprland — динамичный тайловый Wayland-композитор.
* **Композитор:** Wayland — современная альтернатива X11.
* **Терминал:** Kitty — быстрый терминал с GPU-ускорением.
* **Запускатор приложений:** Wofi — нативный launcher для Wayland.
* **Панель:** Waybar — высоко настраиваемая и гибкая статус-панель.
* **Уведомления:** Mako — легкий демон уведомлений.
* **Менеджер обоев:** Hyprpaper — нативный менеджер обоев.
* **Генератор цветовых схем:** Pywal — инструмент для генерации и применения тем по обоям.

## 💻 Установка

### 1️⃣ Клонируйте репозиторий

```bash
git clone https://github.com/teraxms/hyprland-X.git
cd hyprland-X
```

### 2️⃣ Установите зависимости

```bash
sudo pacman -S hyprland kitty wofi waybar mako zsh ttf-jetbrains-mono ttf-jetbrains-mono-nerd hyprpaper python-pipx
pipx install pywal
```

### 3️⃣ Скопируйте конфигурационные файлы

```bash
cp -r .config/* ~/.config/
```

### 4️⃣ Проверьте путь к теме wal

В файлах Waybar/Wofi должен быть импорт:

```css
@import "/home/USERNAME/.cache/wal/colors-waybar.css";
```

> Замените `USERNAME` на ваше имя пользователя.

### 5️⃣ Перезапустите сессию

Теперь вы можете наслаждаться своей новой конфигурацией 😎

---

## 🔄 Обновление
## 🆕 v2.0 - Oбновление

**Новые функции:**
- 📶 Умное управление Wi-Fi
- 🛠 Кнопки в Waybar: Перезагрузка/Выключение/Выход
- 📊 Скорость сети в реальном времени
Если у вас уже установлена старая версия, обновите её:

```bash
cd ~/hyprland-X && ./update.sh
```

После обновления перезапустите Hyprland:

```bash
hyprctl reload
```

---

## 🎨 Кастомизация

Редактируйте конфигурационные файлы в:

```
~/.config
```

Настраивайте темы, панели, хоткеи, эффекты под свои предпочтения.

---

## ⭐ Поддержка

Если конфигурация вам понравилась — поставьте ⭐ на GitHub!
Приятного пользования Hyprland!
